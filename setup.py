from setuptools import setup

setup(name='lppackage',
      version='0.1',
      description='The funniest joke in the world',
      author='F2L',
      license='Zalo',
      packages=['lppackage'],
      zip_safe=False)